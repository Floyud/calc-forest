# product

Deprecated historical product notes.

PM-reviewable source-of-truth documents now live under `docs/specs/`.

Keep this directory as reference material until its useful content is fully migrated or explicitly removed.

Important: this directory still contains pre-rebrand language and old scope assumptions. Do not use it as the current product truth for “我的计算森林”.
