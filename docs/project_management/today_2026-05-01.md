# Today Plan: 2026-05-01

今天目标：把项目从“早期资料齐全”推进到“可以开始写 MVP 代码”的状态。

## 可完成任务

### 1. 建立项目推进中枢

状态：`DONE`

产物：

- `docs/project_management/README.md`
- `docs/project_management/session_protocol.md`
- `docs/project_management/task_board.md`
- `docs/project_management/today_2026-05-01.md`
- `docs/project_management/decision_log.md`

完成标准：

- 能区分 exploration / exploitation / validation。
- 有长期 task board。
- 有今天的任务计划。

### 2. 做一次文档一致性检查

状态：`DONE`

类型：validation

检查范围：

- `docs/product/error_taxonomy.md`
- `docs/engineering/api_plan.md`
- `docs/engineering/data_model.md`
- `docs/engineering/dify_workflow_plan.md`
- `docs/competition/创AI_开发与应用报告_草稿.md`

完成标准：

- API、数据模型、Agent 角色使用同一组概念。
- 诊断标签没有互相冲突。
- 所有输出都保留“教师审核”边界。

### 3. 确认首版演示题型

状态：`DONE`

类型：exploration

建议首版只覆盖：

- 三位数加减法。
- 含 0 的连续退位减法。
- 一位数乘两位数。
- 简单四则混合运算。

完成标准：

- 更新 `docs/product/mvp_scope.md`。
- 明确每类题型至少 3 个错误样例。

### 4. 建立合成数据样例

状态：`DONE`

类型：exploitation

建议产物：

- `development/data/demo_answer_records.json`

数据规模：

- 至少 20 条。
- 覆盖 5 个学生编号。
- 覆盖 E01-E11 中至少 6 类错因。

完成标准：

- 每条记录包含题目、标准答案、学生答案、可选步骤、预期错因。
- 不包含真实学生信息。

### 5. 初始化 FastAPI MVP 骨架

状态：`DONE`

类型：exploitation

建议产物：

```text
development/app/
  main.py
  schemas.py
  services/
    diagnosis.py
    profiles.py
    practice.py
    summaries.py
development/tests/
  test_diagnosis.py
development/data/
  demo_answer_records.json
```

完成标准：

- 使用 `/home/lyzhang/miniconda3/envs/pyt0/bin/python`。
- `GET /health` 可返回状态。
- `POST /api/diagnose` 能对至少 3 条样例返回结构化错因。

### 6. 写首批诊断测试

状态：`DONE`

类型：validation

完成标准：

- 至少测试进位、退位、运算顺序 3 类错因。
- 测试命令可复现。
- 测试结果记录到最终回复或后续日志中。

## 今天优先级

如果时间有限，优先级如下：

1. 文档一致性检查。
2. 确认首版演示题型。
3. 建立合成数据。
4. 初始化 FastAPI 骨架。
5. 写诊断测试。

## 今天不做

- 不接 OCR。
- 不搭完整前端。
- 不做家长端。
- 不接真实学生数据。
- 不追求完整 Dify 部署，只把接口和工作流协议准备好。

## 建议下一次 session

下一次适合开一个 **BUILD-20260501-diagnosis-api** exploitation session：

目标：

- 创建 `development/app/`, `development/data/`, `development/tests/`。
- 写 20 条合成作答记录。
- 实现 `/api/diagnose` 的最小可用版本。
- 跑通 `pytest`。
